<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test24"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="15" />
    <!-- インターネット通信の許可ここから -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--インターネット接続確認 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- インターネット通信の許可ここまで -->
    <!-- ロケーションサービス利用 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- ロケーションサービス利用ここまで -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.test24.Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.test24.Home" android:label="@string/app_name"></activity>
        <activity android:name="com.example.test24.Member_entry" android:label="@string/app_name"></activity>
        <activity android:name="com.example.test24.Dmap" android:label="@string/app_name"></activity>
        <activity android:name="com.example.test24.D_entry" android:label="@string/app_name"></activity>
        <activity android:name="com.example.test24.W_Select" android:label="@string/app_name"></activity>
        <activity android:name="com.example.test24.W_Random" android:label="@string/app_name"></activity></application>

</manifest>
